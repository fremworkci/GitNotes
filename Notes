git config --global user.name "Suman"
git config --global user.email "suman@gmail.com"
git init
git add .
git commit -m "Comment Here"
git remote add origin ssh code paste here
ssh-keygen -t rsa -b 4096 -C "suman@gmail.com"
Public Key path copy kare
cat public key paste here
ssh code copy and go to github setting ssk key
git push origin master




for igonore
git config --global user.name "Suman"
git config --global user.email "suman@gmail.com"
touch .gitignore
open gitignore
for folder:- foldername/
